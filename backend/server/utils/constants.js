const SERVER_PORT = 8080;
const SERVER_URL = `http://localhost:${SERVER_PORT}`;
const PUBLIC_IMAGES_URL = `${SERVER_URL}/public/assets/images`;

module.exports = {
  SERVER_PORT,
  SERVER_URL,
  PUBLIC_IMAGES_URL,
};

